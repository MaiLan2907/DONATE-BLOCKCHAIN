{
  "name": "donate-blockchain-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for Donate Blockchain - manages withdraw requests, withdrawal history, and proofs of fund usage.",
  "main": "src/server.js",
  "scripts": {
    "start": "node --experimental-sqlite --no-warnings=ExperimentalWarning src/server.js",
    "dev": "nodemon --exec \"node --experimental-sqlite --no-warnings=ExperimentalWarning\" src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": ">=22.5"
  }
}
